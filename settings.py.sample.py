from constants import EDITORS
ROOT_DEV_FOLDER = 'your-dev-root-folder'


codebases = {
    "project_name": {
        "valid_commands": [""],
        "folder": "your-project-folder(inside root dev folder)",
        'editor': "editor of choice of project (optional)"
    }
}